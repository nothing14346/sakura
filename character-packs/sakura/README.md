# 夜乃桜角色卡-only 包

本目录提供不含语音模型的夜乃桜角色包：

- `sakura.card.char`

该 `.char` 仅包含角色卡、角色清单和立绘资源，不包含 GPT-SoVITS 语音权重、语音参考音频或 `.voice` 包内容。适合想先从无语音模型状态开始导入、调试或体验角色卡的用户。

导入方式：

1. 打开 Sakura 设置。
2. 进入角色页。
3. 点击“导入 .char”。
4. 选择 `sakura.card.char`。

导入后该角色没有语音包，TTS 可保持关闭；如果后续需要语音，再单独导入对应 `.voice` 语音包。
